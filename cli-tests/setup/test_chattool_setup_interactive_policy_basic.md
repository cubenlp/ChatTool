# chattool setup 交互策略基础用例

## 元信息

- 命令：`chattool setup <command> [-i/-I]`
- 目的：验证交互策略的默认行为、强制交互与缺参终止逻辑。
- 标签：`cli`
- 前置条件：无
- 环境准备：无需外部依赖。
- 回滚：无。
- 来源：`tests/test_setup_interactive_policy.py`

## 状态

- TODO：需要确认真实 CLI 链路的可触达命令与最小可复现路径。

## 用例 1：默认交互策略

### 初始环境准备

- 模拟 Click 上下文来源为默认值。

### 相关文件

- 无

### 预期过程和结果

1. 调用交互策略的归一化逻辑，预期保持为自动模式（返回 None）。

### 参考执行脚本（伪代码）

```sh
chattool setup <command>
```

### 清理 / 回滚

- 无。

## 用例 2：自动提示策略

### 初始环境准备

- 模拟无上下文且支持交互的环境。

### 相关文件

- 无

### 预期过程和结果

1. 解析交互模式，预期可提示、自动交互为真、需要提示为真。

### 参考执行脚本（伪代码）

```sh
chattool setup <command>
```

### 清理 / 回滚

- 无。

## 用例 3：无 TTY 强制交互终止

### 初始环境准备

- 设置为不可提示的环境。

### 相关文件

- 无

### 预期过程和结果

1. 强制交互但无 TTY，预期触发 Abort。

### 参考执行脚本（伪代码）

```sh
chattool setup <command> -i
```

### 清理 / 回滚

- 无。

## 用例 4：缺参且不可交互终止

### 初始环境准备

- 设置为不可提示的环境。

### 相关文件

- 无

### 预期过程和结果

1. 参数缺失且不可交互，预期触发 Abort。

### 参考执行脚本（伪代码）

```sh
chattool setup <command>
```

### 清理 / 回滚

- 无。

## 用例 5：命令定义包含 -i/-I

### 初始环境准备

- 读取 setup 命令元数据。

### 相关文件

- 无

### 预期过程和结果

1. 读取命令元素，预期包含 `-i/-I` 选项。

### 参考执行脚本（伪代码）

```sh
chattool setup frp --help
```

### 清理 / 回滚

- 无。
