interface:
  display_name: "Practice Make Perfact"
  short_description: "Normalize ChatTool work through PR handoff"
  default_prompt: "Use $practice-make-perfact after the ChatTool task works to update project records, normalize durable artifacts, run $chattool-dev-review, and finish PR handoff."
