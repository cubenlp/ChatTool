interface:
  display_name: "Practice Make Perfact"
  short_description: "Light-start ChatTool workflow through PR stage"
  default_prompt: "Use $practice-make-perfact to keep this ChatTool task lightly guided during exploration, then drive review and chattool gh PR creation once the direction is clear."
